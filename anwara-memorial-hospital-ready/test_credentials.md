# Test credentials

Admin email: `admin@anwarahospital.com`

For safety, the password is not stored in the application source. Set `ADMIN_PASSWORD` in the environment before running the seed command.

Role: `super_admin`
