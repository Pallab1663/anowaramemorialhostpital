import {createContext,useContext}from"react";import type{ReactNode}from"react";import{X}from"lucide-react";import{cn}from"./utils";
const C=createContext<(()=>void)|null>(null);
export function Dialog({open,onOpenChange,children}:{open:boolean;onOpenChange?:(open:boolean)=>void;children:ReactNode}){if(!open)return null;return <C.Provider value={()=>onOpenChange?.(false)}><div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/50 p-4" onMouseDown={e=>{if(e.target===e.currentTarget)onOpenChange?.(false)}}>{children}</div></C.Provider>}
export function DialogContent({className,children,...p}:{className?:string;children:ReactNode}){const close=useContext(C);return <div className={cn("relative max-h-[90vh] w-full max-w-md overflow-y-auto rounded-2xl border bg-white p-6 shadow-2xl",className)} {...p}>{children}<button type="button" className="absolute right-4 top-4 rounded-md p-1 text-slate-400 hover:bg-slate-100" onClick={close} aria-label="Close"><X size={18}/></button></div>}
export function DialogHeader({className,children}:{className?:string;children:ReactNode}){return <div className={cn("mb-5",className)}>{children}</div>};export function DialogTitle({className,children}:{className?:string;children:ReactNode}){return <h2 className={cn("text-lg font-semibold",className)}>{children}</h2>};export function DialogFooter({className,children}:{className?:string;children:ReactNode}){return <div className={cn("mt-6 flex justify-end gap-2",className)}>{children}</div>}
